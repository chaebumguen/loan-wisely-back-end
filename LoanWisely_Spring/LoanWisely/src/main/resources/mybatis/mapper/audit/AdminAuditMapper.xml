<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ccksy.loan.domain.audit.mapper.AdminAuditMapper">

    <!-- MG_RE_003: 운영 감사 요약(dateRange) -->
    <select id="selectAdminAuditSummary" parameterType="map" resultType="java.util.HashMap">
        <![CDATA[
        SELECT
            COUNT(*) AS total_events,
            MIN(created_at) AS from_at,
            MAX(created_at) AS to_at
        FROM ACCESS_AUDIT_LOG
        WHERE created_at >= TO_TIMESTAMP(#{fromAt}, 'YYYY-MM-DD HH24:MI:SS')
          AND created_at <  TO_TIMESTAMP(#{toAt},   'YYYY-MM-DD HH24:MI:SS')
        ]]>
    </select>

</mapper>
