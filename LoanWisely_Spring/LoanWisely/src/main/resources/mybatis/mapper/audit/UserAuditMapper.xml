<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ccksy.loan.domain.audit.mapper.UserAuditMapper">

    <!-- MG_RE_002: 본인 접근 감사 로그 (page/size) -->
    <select id="selectMyAccessLogsPage" parameterType="map" resultType="java.util.HashMap">
        <![CDATA[
        SELECT
            log_id,
            user_id,
            resource_type,
            resource_id,
            action,
            ip_address,
            user_agent,
            created_at
        FROM ACCESS_AUDIT_LOG
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        OFFSET #{offset} ROWS FETCH NEXT #{size} ROWS ONLY
        ]]>
    </select>

</mapper>
