<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ccksy.loan.domain.admin.auth.mapper.AdminUserMapper">

    <resultMap id="AdminUserResultMap" type="com.ccksy.loan.domain.admin.auth.entity.AdminUser">
        <id     property="adminId"    column="admin_id"/>
        <result property="username"   column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="status"     column="status"/>
        <result property="createdAt"  column="created_at"/>
    </resultMap>

    <select id="selectByUsername" parameterType="string" resultMap="AdminUserResultMap">
        SELECT *
          FROM ADMIN_USER
         WHERE username = #{username}
    </select>

    <select id="selectRolesByAdminId" parameterType="long" resultType="string">
        SELECT role_name
          FROM ADMIN_USER_ROLE
         WHERE admin_id = #{adminId}
         ORDER BY role_name
    </select>

</mapper>
